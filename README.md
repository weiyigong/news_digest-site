# Weiyi's AI Daily News

This public repository contains only the compiled, sanitized website snapshot
for Weiyi's AI Daily News. The private source repository performs collection,
curation, validation, and publication.

The website is deployed from `site/` by GitHub Pages. Operational state, raw
browser captures, logs, transcripts, credentials, and private source material
are intentionally excluded.
